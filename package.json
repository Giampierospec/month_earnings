{
  "name": "monthly_earnings",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm start --prefix server",
    "dev:client": "npm start  --prefix client",
    "dev:server": "npm run dev --prefix server",
    "install": "NPM_CONFIG_PRODUCTION=false npm run install:server && npm run install:client",
    "install:server": "npm i --prefix server",
    "install:client": "npm i --prefix client",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm run build --prefix server && npm run build --prefix client ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": "^14.17.1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
